# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request

jobs:
  firebase-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
        with:
          node-version: 12.x
      - run: |
          touch .env
          echo VUE_APP_FIREBASE_API_KEY=${{ secrets._VUE_APP_FIREBASE_API_KEY }} >> .env
          echo VUE_APP_FIREBASE_AUTH_DOMAIN=${{ secrets._VUE_APP_FIREBASE_AUTH_DOMAIN }} >> .env
          echo VUE_APP_FIREBASE_PROJECT_ID=${{ secrets._VUE_APP_FIREBASE_PROJECT_ID }} >> .env
          echo VUE_APP_FIREBASE_STORAGE_BUCKET=${{ secrets._VUE_APP_FIREBASE_STORAGE_BUCKET }} >> .env
          echo VUE_APP_FIREBASE_MESSAGE_SENDER_ID=${{ secrets._VUE_APP_FIREBASE_MESSAGE_SENDER_ID }} >> .env
          echo VUE_APP_FIREBASE_APP_ID=${{ secrets._VUE_APP_FIREBASE_APP_ID }} >> .env
          echo VUE_APP_FIREBASE_MEASUREMENT_ID=${{ secrets._VUE_APP_FIREBASE_MEASUREMENT_ID }} >> .env
          cat .env
      - run: npm ci
      - run: npm run build
      - uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project 760199546063
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}